export type Theme = {
  colors: {
    primary: string
    accent: string
    background: string
    divider: string
  }
  fontColors: {
    normal: string
    muted: string
  }
  transparencies: {
    positive: string
    negative: string
  }
}
